---
import MobileNav from "./MobileNav";
import type { PublicLink } from "../types/site";
import type { SiteUiCopy } from "../data/ui";

type Props = {
  navigation: PublicLink[];
  ui: SiteUiCopy["header"];
};

const { navigation: navItems, ui } = Astro.props;
---

<script is:inline>
  document.documentElement.classList.add("js");
</script>
<header class="site-header">
  <div class="header-shell material">
    <a class="brand brand-signature pressable" href="#conteudo" aria-label={ui.homeLabel}>
      <span class="brand-signature__given">Leonardo</span>
      <span class="brand-signature__surname">Candiani</span>
    </a>
    <nav class="desktop-nav" aria-label={ui.primaryNavLabel}>
      {navItems.map((item) => (
        <a class="pressable" href={item.href}>{item.label}</a>
      ))}
      <a
        class="language-switch pressable"
        href={ui.languageHref}
        lang={ui.languageHref === "/" ? "pt-BR" : "en"}
        aria-label={ui.languageLabel}
      >{ui.languageCode}</a>
    </nav>
    <div class="mobile-controls">
      <a
        class="language-switch mobile-language-switch pressable"
        href={ui.languageHref}
        lang={ui.languageHref === "/" ? "pt-BR" : "en"}
        aria-label={ui.languageLabel}
      >{ui.languageCode}</a>
      <details class="mobile-nav-fallback">
        <summary class="nav-trigger pressable material">{ui.menuLabel}</summary>
        <nav aria-label={ui.noJsNavLabel}>
          {navItems.map((item) => (
            <a class="pressable" href={item.href}>{item.label}</a>
          ))}
          <a class="pressable" href={ui.languageHref} lang={ui.languageHref === "/" ? "pt-BR" : "en"}>{ui.languageLabel}</a>
        </nav>
      </details>
      <MobileNav items={navItems} ui={ui} client:load />
    </div>
  </div>
</header>

<style is:global>
  .site-header {
    position: fixed;
    top: 1rem;
    right: 0;
    left: 0;
    z-index: 50;
    padding-inline: var(--space-page);
    pointer-events: none;
  }

  .header-shell {
    display: flex;
    align-items: center;
    justify-content: flex-start;
    gap: clamp(.5rem, 1vw, .75rem);
    width: max-content;
    max-width: 100%;
    min-height: 3.25rem;
    margin-inline: auto;
    padding: .375rem;
    border: 1px solid rgb(23 21 19 / 10%);
    border-radius: var(--radius-control);
    background-color: rgb(251 250 247 / 72%);
    box-shadow: 0 1rem 2.75rem rgb(23 21 19 / 10%);
    -webkit-backdrop-filter: blur(22px) saturate(1.25);
    backdrop-filter: blur(22px) saturate(1.25);
    pointer-events: auto;
  }

  .brand,
  .desktop-nav a,
  .nav-trigger,
  .nav-close {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 2.75rem;
    border-radius: var(--radius-control);
    color: var(--color-ink);
    font-size: .9rem;
    font-weight: 600;
    letter-spacing: 0;
    line-height: 1;
    text-decoration: none;
    white-space: nowrap;
  }

  .site-header .brand {
    align-items: center;
    padding-inline: 1rem;
    border-radius: 0;
    background: transparent;
  }

  .site-header .brand-signature__surname {
    margin-left: .3em;
  }

  .site-header .brand-signature__surname::before {
    display: none;
  }

  .desktop-nav {
    display: flex;
    align-items: center;
    gap: .125rem;
  }

  .desktop-nav a {
    padding-inline: .72rem;
    color: var(--color-graphite);
  }

  .desktop-nav .language-switch {
    margin-left: .25rem;
    border-left: 1px solid var(--color-line);
    border-radius: 0 var(--radius-control) var(--radius-control) 0;
  }

  .site-header .mobile-language-switch {
    display: none;
  }

  .mobile-controls {
    display: none;
  }

  .desktop-nav a:hover,
  .nav-trigger:hover,
  .nav-close:hover {
    background-color: rgb(23 21 19 / 6%);
    color: var(--color-ink);
  }

  .mobile-nav,
  .mobile-nav-fallback {
    display: none;
  }

  .nav-trigger,
  .nav-close {
    border: 1px solid var(--color-line);
    background-color: rgb(251 250 247 / 72%);
    cursor: pointer;
    font-family: inherit;
  }

  .nav-trigger {
    padding-inline: 1rem;
  }

  .mobile-nav-fallback {
    position: relative;
  }

  .mobile-nav-fallback summary {
    list-style: none;
    cursor: pointer;
  }

  .mobile-nav-fallback summary::-webkit-details-marker {
    display: none;
  }

  .mobile-nav-fallback nav {
    position: absolute;
    top: calc(100% + .75rem);
    right: 0;
    display: grid;
    width: min(78vw, 18rem);
    padding: .5rem;
    border: 1px solid var(--color-line);
    border-radius: .75rem;
    background-color: var(--color-paper);
    box-shadow: var(--shadow-float);
  }

  .mobile-nav-fallback a {
    display: flex;
    align-items: center;
    min-height: 3rem;
    padding-inline: .9rem;
    border-radius: .5rem;
    color: var(--color-ink);
    font-size: 1rem;
    font-weight: 650;
    letter-spacing: 0;
    text-decoration: none;
  }

  .mobile-nav-fallback a:hover {
    background-color: rgb(23 21 19 / 6%);
  }

  .nav-scrim {
    position: fixed;
    top: 0;
    right: 0;
    left: 0;
    z-index: 70;
    display: flex;
    align-items: flex-end;
    justify-content: center;
    height: 100dvh;
    max-height: 100dvh;
    padding: 1rem;
    background-color: rgb(23 21 19 / 28%);
    opacity: 1;
    transition: opacity var(--duration-ui) var(--ease-out);
  }

  .nav-scrim[data-state="closed"] {
    opacity: 0;
    pointer-events: none;
  }

  .nav-sheet {
    width: min(100%, 28rem);
    max-height: calc(100dvh - 2rem);
    overflow-y: auto;
    overscroll-behavior: contain;
    padding: .75rem;
    border: 1px solid var(--color-line);
    border-radius: .75rem;
    background-color: var(--color-paper);
    box-shadow: var(--shadow-float);
    transform: translateY(0);
    transition: transform var(--duration-ui) var(--ease-out);
  }

  .nav-scrim[data-state="closed"] .nav-sheet {
    transform: translateY(100%);
  }

  .nav-close {
    width: 100%;
    margin-bottom: .5rem;
    padding-inline: 1rem;
  }

  .nav-sheet nav {
    display: grid;
    gap: .25rem;
  }

  .nav-sheet a {
    display: flex;
    align-items: center;
    min-height: 3.25rem;
    padding-inline: 1rem;
    border-radius: .5rem;
    color: var(--color-ink);
    font-size: 1.15rem;
    font-weight: 650;
    letter-spacing: 0;
    text-decoration: none;
  }

  .nav-sheet a:hover {
    background-color: rgb(23 21 19 / 6%);
  }

  @media (max-width: 51rem) {
    .site-header {
      top: .75rem;
    }

    .header-shell {
      width: min(100%, 26rem);
      min-height: 2.75rem;
      padding: 0;
      border: 0;
      border-radius: 0;
      background: transparent;
      box-shadow: none;
      -webkit-backdrop-filter: none;
      backdrop-filter: none;
    }

    .brand {
      min-height: 2.75rem;
      padding-inline: .8rem;
      border: 1px solid rgb(23 21 19 / 10%);
      border-radius: .875rem;
      background-color: rgb(251 250 247 / 88%);
      box-shadow: 0 .65rem 1.75rem rgb(23 21 19 / 10%);
      -webkit-backdrop-filter: blur(16px) saturate(1.1);
      backdrop-filter: blur(16px) saturate(1.1);
      font-size: .9rem;
    }

    .site-header .brand-signature__given,
    .site-header .brand-signature__surname {
      font-weight: 650;
    }

    .desktop-nav {
      display: none;
    }

    .mobile-controls {
      display: flex;
      align-items: center;
      margin-left: auto;
      padding-inline: .2rem;
      border: 1px solid rgb(23 21 19 / 10%);
      border-radius: .875rem;
      background-color: rgb(251 250 247 / 88%);
      box-shadow: 0 .65rem 1.75rem rgb(23 21 19 / 10%);
      -webkit-backdrop-filter: blur(16px) saturate(1.1);
      backdrop-filter: blur(16px) saturate(1.1);
    }

    .site-header .mobile-language-switch {
      display: inline-flex;
      min-width: 2.75rem;
      min-height: 2.75rem;
      border-right: 1px solid var(--color-line);
      border-radius: 0;
    }

    .mobile-nav,
    .mobile-nav-fallback {
      flex: 0 0 auto;
    }

    .mobile-nav-fallback {
      display: block;
    }

    html.js .mobile-nav {
      display: block;
    }

    html.js .mobile-nav-fallback {
      display: none;
    }

    .nav-trigger {
      min-height: 2.75rem;
      padding-inline: .85rem;
      border: 0;
      background: transparent;
      font-size: .9rem;
    }
  }

  @media (max-width: 22rem) {
    .brand {
      padding-inline: .7rem;
      font-size: .82rem;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .nav-scrim {
      transition: opacity 120ms linear;
    }

    .nav-sheet,
    .nav-scrim[data-state="closed"] .nav-sheet {
      transform: none;
      transition: none;
    }
  }
</style>
